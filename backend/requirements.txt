# ClipCut AI — Python dependencies

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# YouTube downloader
yt-dlp>=2024.1.0

# AI transcription (runs locally — no API key needed)
openai-whisper>=20231117

# OpenAI API (GPT-4o mini for moment detection)
openai>=1.12.0

# Subtitle rendering (replaces ffmpeg font handling)
pillow>=10.0.0

# Face tracking & smart zoom
opencv-python-headless>=4.8.0

# Utilities
python-multipart>=0.0.9
pydantic>=2.0.0
